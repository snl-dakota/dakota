Blurb::
Limits the maximum rank that is explored during a rank adaptation.

Description::
The \c adapt_rank procedure increments the rank on each iteration, and
the maximum value is limited by \c max_rank

<b> Default Behavior </b>

Max rank is unbounded (adaptive sweep will stop when CV error increases).

See_Also::	model-surrogate-global-function_train-adapt_rank
