Blurb::
Specify the scheduling of concurrent evaluations when Dakota is run in parallel
Description::
When Dakota is run in parallel, the partition type and scheduling for the evaluation
servers are determined automatically. If these settings are undesirable, they may be
overridden by the user using the \c evaluation_scheduling keyword.

The partition type and scheduling are 
Topics::	empty
Examples::
Theory::
Faq::
See_Also::	
