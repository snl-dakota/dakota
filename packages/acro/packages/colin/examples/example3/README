This is a simple example to illustrate the use of acro with a
shell function (written in python) that is called from an XML-driven
acro config file.  This shell function performs an evaluation of a 
constrained design point.

Call:

   ../acro acro.xml
