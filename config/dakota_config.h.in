// $Header

// dakota_config.h.in.  Originally generated from configure.ac by autoheader.
// Bares little resemblance with the original, now that the CMake build is
// becoming less-and-less like an autotools build.
#include <Teuchos_config.h>
#include <pecos_config.h>

// Macro to enable fork
#cmakedefine HAVE_WORKING_FORK 1


// Macro to handle code which depends on PECOS. (unconditional dependency!)
//wjbPOUNDcmakedefine HAVE_PECOS 1

